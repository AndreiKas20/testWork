import {Reducer} from "redux";import {ADD_USERS, CLEAR_USERS} from "./usersReducer";const defaultUsersArr = [{}]export const addedUsersReducer: Reducer = (state = defaultUsersArr, action) => {    switch (action.type) {        case ADD_USERS:            return {...state, users: [...action.payload]}        case CLEAR_USERS:            return {users: []}        default:            return state    }}export const  newUsersArrAction = (payload:string) => ({type: ADD_USERS, payload})export const  newUsersArrClearAction = () => ({type: ADD_USERS})
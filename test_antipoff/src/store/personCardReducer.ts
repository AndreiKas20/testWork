import {Reducer} from "redux";import axios from "axios";const defaultState = {}const CHANGE_CARD = 'CHANGE_CARD'export const personCardReducer: Reducer = (state = defaultState, action) => {    switch (action.type) {        case CHANGE_CARD:            return {...state, card: action.payload}        default:            return state    }}export const changePersonCardAction = (payload:object) => ({type: CHANGE_CARD, payload})export const getFullCard = (id: number) => {    return (dispatch: any) => {        axios.get(`https://reqres.in/api/users/${id}`)            .then((resp) => {                const data = resp.data                dispatch(changePersonCardAction(data))            }).catch(console.log)    }}
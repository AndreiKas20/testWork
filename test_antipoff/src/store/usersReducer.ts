import {Reducer} from "redux";import axios from "axios";const defaultUsersArr = {    users: []}const ADD_USERS = 'ADD_USERS'const GET_USERS = 'GET_USERS'const CLEAR_USERS = 'CLEAR_USERS'export const usersReducer: Reducer = (state = defaultUsersArr, action) => {    switch (action.type) {        case ADD_USERS:            return {...state, users: [...state.users,...action.payload]}        case GET_USERS:            return {users: [...state.users]}        case CLEAR_USERS:            return {users: []}        default:            return state    }}export const changeUsersArr = (payload:string) => ({type: ADD_USERS, payload})export const getUsersAction = () => ({type: GET_USERS})export const clearUsersAction = () => ({type: CLEAR_USERS})export const getUsers = (page: number) => {    return (dispatch: any) => {        axios.get(`https://reqres.in/api/users?page=${page}`)            .then((resp) => {                const data = resp.data.data                dispatch(changeUsersArr(data))            }).catch(console.log)    }}
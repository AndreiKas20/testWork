import {Reducer} from "redux";import axios from "axios";type usersArrType = Array<{}>const defaultUsersArr:usersArrType = [{}]const GET_USERS = 'GET_USERS'export const usersReducer: Reducer = (state = defaultUsersArr, action) => {    switch (action.type) {        case GET_USERS:            return {...state, users: action.payload}        default:            return state    }}export const changeUsersArr = (payload:string) => ({type: GET_USERS, payload})export const getUsers = () => {    return (dispatch: any) => {        axios.get('https://reqres.in/api/users?page=2')            .then((resp) => {                const data = resp.data                dispatch(changeUsersArr(data))            }).catch(console.log)    }}